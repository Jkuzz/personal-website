---
import linkedinIcon from '../assets/linkedin.svg'
import githubIcon from '../assets/Github-logo.svg'

const socialMediaIcons = [
  { img: githubIcon, alt: 'GitHub', url: 'https://github.com/Jkuzz' },
  { img: linkedinIcon, alt: 'LinkedIn', url: 'https://www.linkedin.com/in/jkuzz/' },
]
---

<div class="flex flex-row justify-center gap-8 p-4">
  {
    socialMediaIcons.map((icon) => (
      <a
        target="_blank"
        href={icon.url}
        class="origin-bottom-right transition-all hover:-translate-y-2 hover:rotate-6"
      >
        <img
          class="w-16 "
          src={icon.img}
          alt={icon.alt}
        />
      </a>
    ))
  }
</div>
